# ============================================================================
# Ydsz Plane — CI/CD 完整流水线
# 对标: 字节跳动/美团 Go 项目 CI 标准
# 触发: push/PR 到 main, 手动触发
# ============================================================================
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  GO_VERSION: "1.26"
  NODE_VERSION: "22"

jobs:
  # ------------------------------------------------------------------
  # Job 1: Go 静态分析 + 编译校验
  # ------------------------------------------------------------------
  go-lint-build:
    name: Go Lint & Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: go vet
        run: go vet $(go list ./... | grep -v '/web/node_modules/')

      - name: go build
        run: go build $(go list ./... | grep -v '/web/node_modules/')

      - name: go mod tidy check
        run: |
          go mod tidy -diff
          if [ $? -ne 0 ]; then
            echo "::error::go.mod 与 go.sum 不一致，请运行 go mod tidy 后提交"
            exit 1
          fi

  # ------------------------------------------------------------------
  # Job 2: Go 单元测试（race detector）
  # ------------------------------------------------------------------
  go-test:
    name: Go Test (race)
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: ydsz_app
          POSTGRES_PASSWORD: ydsz_test
          POSTGRES_DB: ydsz_plane_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:8-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      YDSZ_DATABASE_URL: "postgres://ydsz_app:ydsz_test@localhost:5432/ydsz_plane_test?sslmode=disable"
      YDSZ_REDIS_ADDR: "localhost:6379"
      YDSZ_REDIS_PASSWORD: ""
      YDSZ_SERVER_ENV: "development"
      YDSZ_AUTH_JWT_SECRET: "ci-test-secret-key-at-least-32-bytes-long!!"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: init test schema
        run: |
          PGPASSWORD=ydsz_test psql -h localhost -U ydsz_app -d ydsz_plane_test \
            -v ON_ERROR_STOP=1 \
            -f sql/ydsz-plane-init.sql

      - name: go test with coverage
        run: |
          PKGS=$(go list ./... | grep -v '/web/node_modules/' | grep -v '/scripts/')
          go test -race -count=1 -timeout 300s -coverprofile=coverage.out -covermode=atomic $PKGS
          echo "--- 覆盖率摘要 ---"
          go tool cover -func=coverage.out | tail -1

      - name: upload coverage artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: go-coverage-${{ github.run_number }}
          path: coverage.out
          retention-days: 14

  # ------------------------------------------------------------------
  # Job 2a: Coverage Ratchet 门禁（S16 P2-3）
  // 确保 PR 覆盖率不下降（allowed_diff ≥ 0）。
  // 整体覆盖率目标 ≥50%；PR 级别的 diff 不允许负值。
  // ------------------------------------------------------------------
  coverage-gate:
    name: Coverage Gate (≥50%, PR diff ≥0%)
    needs: go-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Run coverage with threshold
        run: |
          PKGS=$(go list ./... | grep -v '/web/node_modules/' | grep -v '/scripts/')
          go test -count=1 -timeout 300s -coverprofile=coverage.out -covermode=atomic $PKGS 2>&1 | tee /tmp/test-output.txt
          echo "--- 覆盖率摘要 ---"
          go tool cover -func=coverage.out | tail -1 | tee /tmp/coverage-summary.txt
          # 提取总覆盖率百分比
          COVERAGE=$(grep -oP 'total:\s+\(statements\)\s+\K[0-9.]+' /tmp/coverage-summary.txt || echo "0")
          echo "Coverage: $COVERAGE%"
          echo "coverage=$COVERAGE" >> $GITHUB_OUTPUT
          # 阈值检查: 50%
          if (( $(echo "$COVERAGE < 50" | bc -l) )); then
            echo "::error::Coverage dropped below threshold: $COVERAGE% < 50%"
            echo "::error::Please add tests for new code paths before merging."
            exit 1
          fi
          echo "✅ Coverage gate passed: $COVERAGE% >= 50%"

      - name: Diff-check: PR coverage must not decrease (S16 ratchet)
        if: github.event_name == 'pull_request'
        run: |
          # 获取 main 分支最新覆盖率作为 baseline
          git fetch origin main --depth=1 2>/dev/null || true
          git stash 2>/dev/null || true
          git checkout origin/main -- . 2>/dev/null || echo "SKIP_BASELINE"
          if [ -f go.mod ]; then
            PKGS=$(go list ./... | grep -v '/web/node_modules/' | grep -v '/scripts/')
            go test -count=1 -timeout 300s -coverprofile=coverage-main.out -covermode=atomic $PKGS 2>/dev/null || echo "BASELINE_BUILD_FAILED=1"
            if [ -f coverage-main.out ]; then
              MAIN_COV=$(grep -oP 'total:\s+\(statements\)\s+\K[0-9.]+' <(go tool cover -func=coverage-main.out | tail -1) || echo "0")
              git checkout HEAD -- .
              git stash pop 2>/dev/null || true
              PKGS=$(go list ./... 2>/dev/null | grep -v '/web/node_modules/' | grep -v '/scripts/')
              go test -count=1 -timeout 300s -coverprofile=coverage-pr.out -covermode=atomic $PKGS 2>/dev/null
              PR_COV=$(grep -oP 'total:\s+\(statements\)\s+\K[0-9.]+' <(go tool cover -func=coverage-pr.out | tail -1) || echo "0")
              DIFF=$(echo "$PR_COV - $MAIN_COV" | bc -l)
              echo "Main coverage: $MAIN_COV%, PR coverage: $PR_COV%, diff: $DIFF%"
              if (( $(echo "$DIFF < 0" | bc -l) )); then
                echo "::error::PR decreased coverage by ${DIFF#-}% ($MAIN_COV% → $PR_COV%). Please add tests for new code."
                exit 1
              fi
              echo "✅ PR coverage diff >= 0 ($DIFF%)"
            fi
          fi
          git checkout HEAD -- . 2>/dev/null || true
          git stash pop 2>/dev/null || true

  # ------------------------------------------------------------------
  # Job 2d: API Contract Sync Check (S16 P0-1)
  # 检查 committed api.generated.ts 与 regenerated 的是否一致
  # 注意：当 swagger 注解覆盖率 <80% 时为 warning（不阻断）
  # ------------------------------------------------------------------
  api-sync-check:
    name: API Contract Sync (gen-diff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - uses: pnpm/action-setup@v3
        with:
          version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"
          cache-dependency-path: web/pnpm-lock.yaml

      - name: Install web dependencies
        run: pnpm install --frozen-lockfile
        working-directory: web

      - name: Install openapi-typescript
        run: pnpm add -D openapi-typescript
        working-directory: web

      - name: Install swag
        run: go install github.com/swaggo/swag/cmd/swag@latest

      - name: Generate swagger + types
        run: |
          swag init -g cmd/api/main.go -o docs/swagger --parseDependency --parseInternal 2>&1 || echo "SWAG_GEN_WARN=1"
          cd web && npx openapi-typescript ../docs/swagger/swagger.yaml -o src/types/api.generated.ts --path-params-as-hash-map false

      - name: Check diff (warning mode until coverage ≥80%)
        run: |
          DIFF=$(git diff --stat web/src/types/api.generated.ts docs/swagger/swagger.yaml 2>/dev/null || true)
          if [ -n "$DIFF" ]; then
            echo "::warning::API contract drift detected. Run 'make gen' locally and commit changes."
            echo "$DIFF"
            # TODO: 注解覆盖率 ≥80% 后将 warning 升级为 error（阻断合并）
            # exit 1
          else
            echo "✅ API contract in sync"
          fi

      - name: Report swagger coverage
        run: |
          PATHS=$(grep -c '^  /api/' docs/swagger/swagger.yaml 2>/dev/null || echo 0)
          echo "::notice::Swagger annotated paths: $PATHS (target: ≥160 for 80% coverage)"

  # ------------------------------------------------------------------
  # Job 2c: API Breaking Change 门禁 — oasdiff（P2-9）
  # 仅 PR 到 main 时运行，检测 swagger 破坏性变更
  # ------------------------------------------------------------------
  api-diff:
    name: API Breaking Change (oasdiff)
    needs: go-test
    runs-on: ubuntu-latest
    if: github.base_ref == 'main' || github.event_name == 'push'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 需要完整历史以对比 main 分支

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: Install swag
        run: |
          go install github.com/swaggo/swag/cmd/swag@latest

      - name: Generate current branch swagger
        run: |
          swag init -g cmd/api/main.go -o /tmp/new-swagger --parseDependency --parseInternal 2>&1 || echo "SWAG_GEN_FAILED=1"

      - name: Fetch main branch swagger baseline
        run: |
          git fetch origin main --depth=1 2>&1 || true
          git show origin/main:docs/swagger/swagger.yaml > /tmp/old-swagger.yaml 2>/dev/null || echo "NO_BASELINE=1"

      - name: Run oasdiff breaking change detection
        run: |
          if [ "$NO_BASELINE" = "1" ] || [ "$SWAG_GEN_FAILED" = "1" ]; then
            echo "::warning::Swagger diff check skipped in this environment"
            echo "Reason: baseline or current swagger generation unavailable."
            echo "This is non-blocking. The team should ensure swagger artifacts are committed."
            exit 0
          fi
          go install github.com/oasdiff/oasdiff@latest 2>&1 || echo "OASDIFF_INSTALL_FAILED=1"
          if [ "$OASDIFF_INSTALL_FAILED" = "1" ]; then
            echo "::warning::oasdiff install failed, skipping breaking change check"
            exit 0
          fi
          NEW_SWAGGER=$(find /tmp/new-swagger -name 'swagger.yaml' -o -name 'swagger.json' | head -1)
          if [ -z "$NEW_SWAGGER" ]; then
            echo "::warning::Generated swagger file not found in /tmp/new-swagger"
            exit 0
          fi
          echo "Comparing: /tmp/old-swagger.yaml → $NEW_SWAGGER"
          oasdiff breaking /tmp/old-swagger.yaml "$NEW_SWAGGER" --fail-on ERR 2>&1 || {
            EXIT_CODE=$?
            if [ $EXIT_CODE -ne 0 ]; then
              echo "::error::API breaking changes detected! Review the diff above."
              exit $EXIT_CODE
            fi
          }
          echo "✅ No API breaking changes detected"

  # ------------------------------------------------------------------
  # Job 2b: Go 深度静态分析（golangci-lint）
  # ------------------------------------------------------------------
  go-linter:
    name: Go Linter (golangci-lint)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: v1.62
          args: --config=.golangci.yml --timeout 5m

  # ------------------------------------------------------------------
  # Job 3: Go 依赖安全扫描
  # ------------------------------------------------------------------
  go-security:
    name: Go Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: govulncheck
        run: |
          go run golang.org/x/vuln/cmd/govulncheck@latest $(go list ./... | grep -v '/web/node_modules/')

  # ------------------------------------------------------------------
  # Job 4: Web 前端审计
  # ------------------------------------------------------------------
  web-audit:
    name: Web Audit
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v3
        with:
          version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "pnpm"
          cache-dependency-path: web/pnpm-lock.yaml

      - name: install deps
        run: pnpm install --frozen-lockfile

      - name: pnpm audit
        run: pnpm audit --prod --audit-level=high

      - name: type check
        run: pnpm type-check
        # S15 P0-5: 去掉历史遗留的 "|| echo" 吞错逻辑
        # CI 门禁承诺（README 声称 CI 全绿）：lint/type-check 失败应阻断流水线

      - name: lint
        run: pnpm lint
        # S15 P0-5: lint 失败立即触发 job failed，max-warnings 0 严格执行

  # ------------------------------------------------------------------
  # Job 4: API 冒烟门禁（启动服务 + 健康检查）
  # ------------------------------------------------------------------
  smoke:
    name: API Smoke (healthz + readyz)
    runs-on: ubuntu-latest
    needs: [go-lint-build]  # 编译通过后再冒烟

    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: ydsz_app
          POSTGRES_PASSWORD: ydsz_test
          POSTGRES_DB: ydsz_plane_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:8-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      YDSZ_DATABASE_URL: "postgres://ydsz_app:ydsz_test@localhost:5432/ydsz_plane_test?sslmode=disable"
      YDSZ_REDIS_ADDR: "localhost:6379"
      YDSZ_REDIS_PASSWORD: ""
      YDSZ_SERVER_ENV: "development"
      YDSZ_AUTH_JWT_SECRET: "ci-test-secret-key-at-least-32-bytes-long!!"
      YDSZ_STORAGE_SECRET_KEY: "ci-test-storage-secret-key-at-least-32-bytes!"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: init database schema
        run: |
          PGPASSWORD=ydsz_test psql -h localhost -U ydsz_app -d ydsz_plane_test \
            -v ON_ERROR_STOP=1 \
            -f sql/ydsz-plane-init.sql

      - name: build API binary
        run: go build -o /tmp/ydsz-api ./cmd/api

      - name: start API server (background)
        run: |
          /tmp/ydsz-api > /tmp/api.log 2>&1 &
          echo "API_PID=$!" >> $GITHUB_ENV
          # 等待服务就绪（最长 30 秒）
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8080/healthz > /dev/null 2>&1; then
              echo "API started after ${i}s"
              break
            fi
            sleep 1
          done

      - name: smoke — /healthz returns 200
        run: |
          RESP=$(curl -sf -w "\n%{http_code}" http://localhost:8080/healthz)
          CODE=$(echo "$RESP" | tail -1)
          if [ "$CODE" != "200" ]; then
            echo "::error::healthz returned HTTP $CODE, expected 200"
            cat /tmp/api.log
            exit 1
          fi
          echo "✅ /healthz → 200 OK"

      - name: smoke — /readyz returns 200 with DB+Redis up
        run: |
          RESP=$(curl -sf -w "\n%{http_code}" http://localhost:8080/readyz)
          CODE=$(echo "$RESP" | tail -1)
          BODY=$(echo "$RESP" | head -1)
          if [ "$CODE" != "200" ]; then
            echo "::error::readyz returned HTTP $CODE (expected 200), body: $BODY"
            cat /tmp/api.log
            exit 1
          fi
          echo "✅ /readyz → 200 OK, body: $BODY"

      - name: stop API server
        if: always()
        run: |
          if [ -n "$API_PID" ]; then kill $API_PID 2>/dev/null || true; fi

  # ------------------------------------------------------------------
  # Job 5: CodeQL 安全分析
  # ------------------------------------------------------------------
  codeql:
    name: CodeQL Analysis
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: ["go", "javascript-typescript"]
    steps:
      - uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          queries: security-and-quality

      - name: Autobuild
        uses: github/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{ matrix.language }}"

  # ------------------------------------------------------------------
  # Job 6: Playwright E2E 测试（仅 main 分支 push 时触发）
  # ------------------------------------------------------------------
  e2e:
    name: Playwright E2E (chromium)
    runs-on: ubuntu-latest    # 仅 main 分支推送时运行，避免 fork PR 服务不可用导致误报
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: go-test
    concurrency: e2e-${{ github.ref }}

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: ydsz_app
          POSTGRES_PASSWORD: ydsz_test
          POSTGRES_DB: ydsz_plane_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      rabbitmq:
        image: rabbitmq:3-management-alpine
        ports:
          - 5672:5672
          - 15672:15672
        options: >-
          --health-cmd "rabbitmqctl status"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      YDSZ_DATABASE_URL: "postgres://ydsz_app:ydsz_test@localhost:5432/ydsz_plane_test?sslmode=disable"
      YDSZ_REDIS_ADDR: "localhost:6379"
      YDSZ_REDIS_PASSWORD: ""
      YDSZ_RABBITMQ_URL: "amqp://guest:guest@localhost:5672/"
      YDSZ_SERVER_ENV: "development"
      YDSZ_AUTH_JWT_SECRET: "ci-test-secret-key-at-least-32-bytes-long!!"
      YDSZ_STORAGE_SECRET_KEY: "ci-test-storage-secret-key-at-least-32-bytes!"

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: install web dependencies
        run: pnpm install --frozen-lockfile

      - name: install Playwright browsers
        run: cd web && npx playwright install --with-deps chromium

      - name: build web frontend
        run: cd web && pnpm build

      - name: init database schema
        run: |
          PGPASSWORD=ydsz_test psql -h localhost -U ydsz_app -d ydsz_plane_test \
            -v ON_ERROR_STOP=1 \
            -f sql/ydsz-plane-init.sql

      - name: build API binary
        run: go build -o /tmp/ydsz-api ./cmd/api

      - name: start API server (background)
        run: |
          /tmp/ydsz-api > /tmp/api.log 2>&1 &
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8080/healthz > /dev/null 2>&1; then
              echo "API started after ${i}s"
              break
            fi
            sleep 1
          done

      - name: run Playwright E2E tests
        run: cd web && pnpm exec playwright test --project=chromium

      - name: stop API server
        if: always()
        run: pkill -f /tmp/ydsz-api 2>/dev/null || true

  # ------------------------------------------------------------------
  # Job 7: k6 Performance Baseline — S16 P2-2
  # 轻量级性能基线验证（仅在 main 分支 push 时运行）
  # 对标 Google SRE P95 < 200ms / 字节 API 规范
  # ------------------------------------------------------------------
  perf-baseline:
    name: k6 Performance Baseline (P95 ≤ 200ms)
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: go-test
    concurrency: perf-${{ github.ref }}

    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_USER: ydsz_app
          POSTGRES_PASSWORD: ydsz_test
          POSTGRES_DB: ydsz_plane_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:8-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      YDSZ_DATABASE_URL: "postgres://ydsz_app:ydsz_test@localhost:5432/ydsz_plane_test?sslmode=disable"
      YDSZ_REDIS_ADDR: "localhost:6379"
      YDSZ_REDIS_PASSWORD: ""
      YDSZ_SERVER_ENV: "development"
      YDSZ_AUTH_JWT_SECRET: "ci-test-secret-key-at-least-32-bytes-long!!"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - name: init database schema
        run: |
          PGPASSWORD=ydsz_test psql -h localhost -U ydsz_app -d ydsz_plane_test \
            -v ON_ERROR_STOP=1 \
            -f sql/ydsz-plane-init.sql

      - name: seed test data
        run: go run ./scripts/seed

      - name: build API binary
        run: go build -o /tmp/ydsz-api ./cmd/api

      - name: start API server
        run: |
          /tmp/ydsz-api > /tmp/api.log 2>&1 &
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8080/healthz > /dev/null 2>&1; then
              echo "API started after ${i}s"
              break
            fi
            sleep 1
          done

      - name: install k6
        run: |
          sudo gpg -k
          sudo gpg --no-default-keyring --keyring /usr/share/keyrings/k6-archive-keyring.gpg --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys C5AD17C747E3415A3642D57D77C6C491D6AC1D69
          echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
          sudo apt-get update
          sudo apt-get install -y k6

      - name: run k6 baseline smoke test
        run: |
          k6 run \
            -e BASE_URL=http://localhost:8080/api/v1 \
            -e TEST_USER_EMAIL=admin@njydsz.com \
            -e TEST_USER_PASSWORD=Admin@1020 \
            --summary-export=/tmp/k6-summary.json \
            tests/perf/post-deploy-check.js
          echo "--- k6 Summary ---"
          cat /tmp/k6-summary.json || true

      - name: stop API server
        if: always()
        run: pkill -f /tmp/ydsz-api 2>/dev/null || true
