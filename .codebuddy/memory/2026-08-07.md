# 2026-08-07 工作日志

## S7 实施收尾

按优先级逐项完成 S7 收尾工作：

### 1. 7.9 性能基线收尾 ✅
- 创建 `docs/perf/README.md` 性能基线报告归档目录
- 创建 `scripts/seed-scale/main.go` 大规模造数脚本（100 万工作项，8 并发 worker，批量 INSERT，断点续传，进度日志）
- 更新 `Makefile` 添加 `seed-scale` 命令

### 2. 7.7 缺陷导出增强 ✅
- 后端 `internal/application/issue/handler.go`：新增 xlsx 格式导出，使用纯 Go 标准库（`archive/zip` + XML 模板），无第三方依赖
- 前端 `web/src/api/services/issue.ts`：`exportUrl` 增加 `format` 参数
- 前端 `web/src/views/project/IssueListView.vue`：导出按钮改为下拉菜单（CSV / Excel）

### 3. 7.8 MVP 加固走查 ✅
- 确认通知铃铛、通知列表、评论列表、看板视图均已覆盖加载态/空态/错误态
- 无需额外补充

### 4. 7.10 发布收尾 ✅
- 更新 `CHANGELOG.md`：细化 S7 交付物描述，新增 S7 收尾条目

### 5. S7 出口检查 ✅
- 按 `09-通知与实时推送设计.md` 逐项核验
- 站内信完整闭环（检查项 2）✅
- WS 断线重连补偿已补全：
  - 前端 `ws-client.ts`：增加 `onReconnect`/`offReconnect` 回调，`lastDisconnectTimestamp` 记录断开时间
  - 前端 `WorkspaceLayout.vue`：注册断线补偿回调
  - 前端 `notification.ts` API/Store：支持 `since` 参数
  - 后端 `notification/service.go`：`ListInput` 增加 `Since` 字段
  - 后端 `notification/handler.go`：支持 `since` 查询参数
- 邮件/IM/摘要（检查项 3-5）归 S9，规则矩阵完整度归后续迭代

### 已知编译问题
- `internal/application/attachment` 包编译失败（预存问题）
- `internal/application/workspace/riskrules.go:83` 引用 `pgx.CommandTag` 不存在
- S7 核心包（issue/notification/attachment）均可独立编译通过
