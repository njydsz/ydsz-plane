# 2026-08-07

## 任务：全项目逐文件夹完善注释（对标大厂标准）

对 Go 后端（cmd/internal/pkg）与 Web 前端（web/src）的全部源文件进行了注释完善。通过 code-explorer 子代理全量盘点后确认：**本项目注释质量本就极高（约 90% 文件已是"优秀"级）**，实际只需补齐少量缺口。

### 本次修改的文件
**Go（补齐 package 级注释 / handler 函数注释）：**
- 新增 `// Package ...` 包文档：`attachment/handler.go`、`notification/handler.go`、`notification/service.go`、`issue/comment_service.go`、`issue/notify.go`、`sprint/notify.go`
- 规范化包注释：`auth/helpers.go`（原为"本文件属于 Package"）
- 补齐 handler 单条 doc 注释：`http/handlers.go`（全部 16 个 handler 函数）、`http/router.go`（forgotPassword/resetPassword）

**Web 前端（补齐文件级/函数级注释）：**
- 完全无注释的 store 重写加注释：`stores/notification.ts`、`stores/search.ts`
- 补文件头+类型注释：`api/services/notification.ts`、`search.ts`、`workbench.ts`、`auth.ts`
- 补文件级+函数注释：`components/GlobalSearch.vue`、`NotificationBell.vue`、`HomeView.vue`、`App.vue`
- 补算法行内注释：`components/MiniGantt.vue`
- 补 action 注释：`stores/issue.ts`

### ⚠️ 重要发现：attachment 包处于"半迁移、未提交、编译失败"状态
- `internal/application/attachment/` 在**工作区中存在未提交的重构**（本次任务之前就存在，非我引入）。
- `models.go` 是 **untracked** 文件，重新声明了 `Attachment`/`CreateInput`，与 `service.go` 冲突（redeclared）。
- `service.go`/`handler.go` 已改为"预签名直传"方案，删除了 `Create`/`Get`/`ListByEntity`/`download` 等方法，但 `handler.go` 仍调用已删除的方法（`ListByEntity`、`Create`、`Get`），且 `WithDetails([]errs.FieldDetail{...})` 参数类型不匹配。
- **该包当前无法编译**（`go build`/`go vet` 报错均集中在此包，其余包均通过）。修复它超出"加注释"任务范围，**未改动其工作代码**，需用户另行处理。

### 项目约定（写入 MEMORY.md）
- 注释语言统一中文；Go 用 `// Package xxx ...` + 每个导出符号 doc + 详尽行内注释。
- Web 前端用 `/** */` JSDoc + `<template>` 顶部 `<!-- 文件级说明 -->`。
- 详细注释风格可参考 `internal/config/config.go`、`internal/application/version/service.go`、`web/src/stores/sprint.ts`。

---

## 任务二：工程化治理补强（对标竞品/大厂规范，P0-P2 全部落地）

在上一任务（注释完善）之后，针对现状差距补齐了 CI/CD、测试、版本发布、安全、性能治理。**注：此任务中发现 attachment 包已编译通过（上一条记录提到的编译失败已被后续提交 9749508/5901d83 修复）。**

### P0 止血
- 创建 `.github/workflows/ci.yml`：后端 go-lint / go-test(race+覆盖率门禁 10%) / go-security(govulncheck)，前端 web-lint / web-test-build / web-audit，code-review(ocr，continue-on-error)
- 清理 `web/_tmp_2736_...` 误跟踪空文件（git rm）；.gitignore 增加 `_tmp_*`、`web/coverage/`、`playwright-report/`、`test-results/`、`/coverage`、`coverage.html`
- 前端去掉 `--passWithNoTests`，改为强制 vitest
- 修复 web/package.json `packageManager`：`pnpm@10.7.0` → `pnpm@10.27.0`（本机 corepack 切换失败根因，10.7.0 bin 缺失）
- 前端新增 devDeps：@vue/test-utils、happy-dom、@vitest/coverage-v8、@playwright/test

### P1 加固
- vite.config.ts 增 vitest test 块（happy-dom + 覆盖率阈值 lines50/func40/branch40/stat50）
- 5 个组件单测：AppButton/AppAvatar/AppBadge/ProgressBar/AppErrorState
- Playwright 配置（web/playwright.config.ts）+ 鉴权 E2E（web/e2e/auth.spec.ts）
- CHANGELOG.md、docs/architecture/15-发布管理.md、.github/workflows/release.yml
- 4 个 workflow：ci.yml / codeql.yml / perf.yml / release.yml
- .opencodereview/rule.json（OCR 项目级审查规则：SQL 注入、RLS 越权、事务边界、N+1、XSS）
- docs/architecture/16-依赖安全治理.md、17-性能基线.md
- Makefile 增 coverage / coverage-html / perf / perf-json 目标

### P2 测试补写（纯逻辑，全部通过，go vet 通过）
- pkg/errs：覆盖率 37.5→83.3%（构造/JSON 序列化/错误链）
- middleware：0→24.1%（parseBigInt/bearerToken 优先级/SecurityHeaders/workspace param）
- workspace：0→8.5%（normalizeProjectSlug/normalizeIdentifier）
- attachment：0%（sanitizeFilename 防路径穿越）
- search：0%（toTSQuery 防注入/buildDocURL）
- notification：0→1.5%（inDNDWindow/defaultPreference/EventTitles）

### ⚠️ 环境限制（重要，跨会话需知）
- **本机 web/node_modules 物理损坏**：`.pnpm/vitest@2.1.9` 目录为空、vite peer 未链接，`pnpm test` 报 ERR_MODULE_NOT_FOUND。pnpm install 反复报 SAFE_DELETE / UNKNOWN open 错误。**需删除 web/node_modules 后重新 `pnpm install`**（CI 的 Linux 干净环境用 `--frozen-lockfile` 正常）。
- 本机无 cgo，`go test -race` 不可用（CI Linux 自动启用 cgo，正常）。
- govulncheck 因本机网络不可达（proxy.golang.org 连不上）未能本地安装，CI 环境可运行。
- 后端总体覆盖率约 10%，CI 门槛设为渐进式 10%，目标 40%+。
- 用户拒绝了一次 node_modules 修复命令，**不要再用删除/强制重建 node_modules 的变通手段**。
